var b = Object.defineProperty;
var i = (a, e) => b(a, "name", { value: e, configurable: !0 });
function p(a, e, t, s, v, n, f) {
  if (v === 0)
    return !1;
  var r = v, u = 0, o = a;
  if (f > e + r && f > s + r || f < e - r && f < s - r || n > a + r && n > t + r || n < a - r && n < t - r)
    return !1;
  if (a !== t)
    u = (e - s) / (a - t), o = (a * s - t * e) / (a - t);
  else
    return Math.abs(n - a) <= r / 2;
  var _ = u * n - f + o, l = _ * _ / (u * u + 1);
  return l <= r / 2 * r / 2;
}
i(p, "containStroke");
export {
  p as containStroke
};
