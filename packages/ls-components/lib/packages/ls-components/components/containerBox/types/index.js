import { buildProps as t } from "../../_utils/runtime/index.js";
const o = t({
  // 宽度
  width: {
    type: Number,
    default: 1920
  },
  // 高度
  height: {
    type: Number,
    default: 1080
  }
});
export {
  o as lsContainerBoxProps
};
